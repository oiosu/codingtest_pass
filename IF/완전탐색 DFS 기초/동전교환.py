from sklearn.utils import resample


def DFS(L, sum):
    global res  
    if L > res:
        return 
    if sum > m:
        return 
        if L < res:
            res = L
    else:
        for i in range(n):
            # DFS(L+1, sum+ a[i]) 이부분 다시 체크하기 